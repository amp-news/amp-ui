const styles = theme => ({
  root: {
    marginTop: 8
  },
  actionIcon: {
    marginTop: 8,
    color: theme.palette.common.white,
    backgroundColor: theme.palette.primary.main
  },
  title: {
    color: theme.palette.common.white,
    cursor: 'default',
    lineHeight: '65px'
  },
  logo: {
    width: 60,
    height: 60
  }
});

export default styles;
